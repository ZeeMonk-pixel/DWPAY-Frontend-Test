# Vue 3 User Management Module

A clean, component-based User Management module built with **Vue 3,
Vite, Pinia, and Vue Router**.

This project demonstrates modern frontend best practices including state
management, optimistic UI updates, reactive computed properties, and
reusable components.

------------------------------------------------------------------------

## 🚀 Tech Stack

-   Vue 3 (Composition API)
-   Vite
-   Pinia (State Management)
-   Vue Router
-   Custom CSS (No UI libraries)
-   Mocked API Layer

------------------------------------------------------------------------

## ✨ Key Features

-   Fetch and display users (mocked API)
-   Loading and error states
-   Case-insensitive search
-   Create user with validation
-   Optimistic UI updates
-   Reactive Admin user count
-   Success toast notification
-   State persists across navigation

------------------------------------------------------------------------

## 🧠 Architecture Highlights

-   Clear separation of concerns (API → Store → Views → Components)
-   Reusable components (`UserForm`, `UserTable`, `Toast`, etc.)
-   Centralized state management with Pinia
-   Computed properties for automatic reactivity
-   Scalable structure suitable for real-world applications

------------------------------------------------------------------------

## 📦 Run Locally

Install dependencies:

``` bash
npm install
```

Start development server:

``` bash
npm run dev
```

Build for production:

``` bash
npm run build
```

------------------------------------------------------------------------

## 🎯 Purpose

Built as a technical assessment to demonstrate:

-   Clean code structure
-   Modern Vue 3 patterns
-   UX considerations
-   Scalable frontend architecture
